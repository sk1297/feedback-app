# Production Configuration for Render
server.port=${PORT:8080}
server.servlet.context-path=

# Database Configuration - Will be replaced with environment variables
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/feedback_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=true

# Mail Configuration - Use environment variables
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:swapnilks1297@gmail.com}
spring.mail.password=${MAIL_PASSWORD:pmsqvqjzrelhtncr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Custom Application Properties
feedback.email.to=${FEEDBACK_EMAIL_TO:adeshdhage5@gmail.com}
feedback.email.subject=Daily Feedback Report
feedback.email.from=${FEEDBACK_EMAIL_FROM:swapnilks1297@gmail.com}

# Logging Configuration for Production
logging.level.com.feedback.system=INFO
logging.level.org.springframework.web=WARN
logging.level.org.springframework.mail=WARN

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Production Profile
spring.profiles.active=prod